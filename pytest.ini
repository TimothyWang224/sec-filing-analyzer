[pytest]
# Explicitly set the test paths to avoid conflicts with external_libs
testpaths = tests
# Ignore the external_libs directory for test collection
norecursedirs = external_libs .* venv .venv build dist
